/**
 * 全局常量
 * 后端 blueblood-api：http://localhost:8090，context-path=/api
 * 小程序请求 baseURL = http://localhost:8090/api
 * 注意：真机调试请改成可访问的内网/公网地址，并在微信后台配置 request 合法域名。
 */
export const BASE_URL = 'http://localhost:8090/api'

/** 本地存储 key */
export const TOKEN_KEY = 'token'
