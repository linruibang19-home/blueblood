# 09-后端接口验收清单

## 任务目标

对第二阶段 Spring Boot API 进行整体验收。

## 验收内容

基础：

```text
项目可启动
Swagger/Knife4j 可访问
MySQL 连接正常
Redis 连接正常
统一响应正常
统一异常正常
登录认证正常
```

业务：

```text
用户资料接口可用
社区接口可用
课程作业接口可用
任务里程碑接口可用
钱包通知接口可用
文件上传接口可用
```

前台联调：

```text
前台 H5 可切换 Mock/API
核心页面能读取真实接口
任务接单流程可走通
里程碑提交可走通
收益和通知可查询
```

Docker：

```text
Spring Boot Dockerfile 可构建
docker-compose 可启动 api/mysql/redis
```

## 交给 AI IDE 的执行提示

```text
请只对后端接口做验收和小范围修复。不要新增后台管理端或小程序端。
```

